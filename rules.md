# Graphic-KataGo

本项目为 **Graphic-KataGo**，缩写 **GKT Go**（故代码与模块名为 `gkt` / `Gkt*`）。

# 规则

本文件只定义**拓展版围棋**本身：盘、着法、禁手、终局、计分。  
五子棋与反五子棋（网格几何连珠；用来检验 **2DCNN 完备性**，对照图围棋检验 **GNN 完备性**）写在 [`gomoku.md`](gomoku.md)，不改写本节。  
启发见 §启发，**不进入**合法着与计分。算法与前端见 [`algorithm.md`](algorithm.md)；训练分阶段见 [`training_method.md`](training_method.md)。引擎实现为 `cpp/`（`gkt_native`）。

所有术语按规则各节定义；实现不得另发明规则。

---

## 启发（不是规则）

传统围棋把古代战争收成几条可下的关系：占实地、围歼、切断补给、做眼成根据、劫争局部反复。棋盘是匀质的无向网格，两军对垒，气是四周的空隙——对应的是平原上近距离的接触与合围，而不是现代的航线、网络和单向通道。

本项目用**同一种收法**做两件事：把传统围棋从网格推广到任意有向图；从而把「现代战争」也收成可下的关系，而不是做成兵棋仿真。战场不再假定匀质平面：边可以有方向（补给、打击、通行不必对称），连通可以是乱图（交通、通信、轨道，看起来会像一团乱麻），参战方可多于两方。气改为**空入邻居**，焦点与弱点可以分叉，正是「只从这一侧够得着」的抽象，不是新玩法噱头。

人在乱图上难以靠目视点选来指挥，正是这种抽象的后果，不是 UI 没画好而已。讲解、对话与战术执行（见 [`algorithm.md`](algorithm.md) §10.1）属于实现层，不改本节规则。

另一初衷是**实验**：围棋策略有多少绑在棋盘的形状与拓扑上，有多少是「占子—气—围歼」本身。本节规则与图同构，故 **GNN** 是图围棋的主网（拓扑换成乱图/有向边之后还能不能学）。实现里 **2DCNN** 仍是正经的网格围棋对照网（与传统 AlphaZero/KataGo 式卷积同类，只在有 `.grid` 的盘上训），用来当「形状还在时」图围棋还剩多少。**MLP / 1DCNN 不是认真选手**，几乎不看真邻接，只用来验证 GNN 是否真的用了图结构。网格几何连珠（五子棋 / 反五子棋）反过来检验 **2DCNN 完备性**，见 [`gomoku.md`](gomoku.md)。网络比较在 [`algorithm.md`](algorithm.md) §8。

下面从 §0 起才是对局定义。

---

## 0. 基本对象

- **图** \(G=(V,E)\)，有向图。\(V\) 为顶点集，\(E\subseteq V\times V\) 为有向边集。\(u\to v\) 表示 \((u,v)\in E\)。
- **无向图**作为有向图：每条无向边 \(\{u,v\}\) 同时对应 \((u,v)\) 与 \((v,u)\)。

气、切断、包围都依赖「多数点只有少量入邻居」。**平均入度过高则连通过强**：几乎处处互相够得着，块难被切断，围歼与做眼退化成稠密图上的占点，不再像围棋。合法着不禁止这种图，**不建议**棋盘边过多。网格内部入度约为 4；乱图也应把平均入度压在同一量级，并给入度设硬上限。内置随机图的具体算法见 [`algorithm.md`](algorithm.md) §12。
- **玩家集** \(P\)，\(|P|=k\)（默认 \(k=2\)）。每位玩家有颜色编号。
- **占领函数** \(\mathrm{occ}:V\to P\cup\{\emptyset\}\)。空顶点记 \(\emptyset\)。
- **局面** \(s=(\mathrm{occ},\,p_{\mathrm{move}})\)：占领状态与当前执子方。

---

## 1. 棋盘与行棋

- 玩家轮流行棋，每次选一个未被占领的顶点 \(v\)，令 \(\mathrm{occ}(v):=p\)。
- 一方也可**虚手**（pass，不动），且虚手**始终合法**。
- **出局**：某方在自己的**连续两手上**都虚手 ⇒ 该方**出局**——停止行棋，但**不算输**（棋子与领地计入终局计分）。
- 游戏结束有两种情形（§5.1）：协议结束；或倒数第二人出局。

---

## 2. 块、气与提子

- **块**：同一方占领的、在无向邻接（把有向边对称化）意义下的极大连通子集。
- 一块 \(B\) 的**气**：\(\mathrm{libs}(B)=\{u\in V:\exists v\in B,\,(u,v)\in E,\,\mathrm{occ}(u)=\emptyset\}\)。气只看出边指向块内顶点的**空入邻居**。
- 一块若无气，**立即被提**：其顶点重置为空。提子在每次非虚手落子后处理，且只提**他方**无气块（己方无气见 §4.1 自杀）。多方时可一手数家。

在无向图上入邻居 = 出邻居 = 邻居。在有向图上气仅由入邻居定义，于是焦点 / 弱点 / 眼开始分叉。

---

## 3. 焦点 / 弱点 / 眼

设 \(v\) 为空顶点，\(p\) 为某方。

| 术语 | 定义 |
|---|---|
| **焦点** | \(v\) **至少有一个入邻居**，且所有入邻居都被 \(p\) 占领。无入边的空点不是任何一方的焦点 |
| **弱点** | \(v\) **至少有一个出邻居**，且所有出邻居都被 \(p\) 占领。无出边的空点不是任何一方的弱点 |
| **眼** | \(v\) 同时是 \(p\) 的焦点和弱点 |
| **真焦点** | 焦点且所有入邻居属于 \(p\) 的**同一块** |
| **真弱点** | 弱点且所有出邻居属于 \(p\) 的**同一块** |
| **实眼** | 同时是真焦点与真弱点 |
| **真眼** | 实眼，且全体入+出邻居同属 \(p\) 的**同一块** |

无向图上：焦点 = 弱点 ⇒ 焦点 ≡ 眼；真焦点 ≡ 真弱点 ⇒ 真焦点 ≡ 实眼；再因入+出即全部邻居，实眼 ≡ 真眼。这与古典围棋的「眼」一致。

无入边的空点不是焦点：否则空邻域上全称命题为真，孤立点会被所有方同时当成焦点。引擎扩张时跳过这些点；§5.3 入邻居全空则该点不计分。

无出边的空点不是弱点：同样因为空邻域上「所有出邻居都被占」为真。真弱点以弱点为前提，因此也要求至少有一个出邻居。引擎不计弱点谓词（只做焦点扩张），但定义与焦点对称，避免全称空量化。

用途：

1. **死活**：一块有两个真眼则无法被提（除非己方填眼）。无向图上即两个真焦点。
2. **终局焦点扩张**：结束后各方占领自己的焦点以扩大实空。
3. **做活**：真眼数与潜在眼位决定死活。

规则引擎**只实现焦点扩张**；弱点 / 眼是术语，不作为网络手工特征。

---

## 4. 禁手

### 4.1 自杀

占 \(v\) 使己方某块在提完他方无气块之后仍无气，则**禁止**。该步不改变局面；己方无气块不被提掉。

令 \(s'\) 为只把 \(v\) 改为 \(p\)、尚未提子的占领。先提所有无气的**他方**块（\(k\ge 3\) 时各方分别提）得到 \(s''\)。\(s''\) 中每一个己方块都必须至少有一口气。

有向图上被提顶点未必是己方空入邻居，提子不必给己方补气。无向图上填对方最后一口气总会给新子至少一口气。

### 4.2 超劫（局面超级劫 SSK）

- 历史集 \(H\) 记录所有**落子前**出现过的 \((\mathrm{occ},\,p_{\mathrm{move}})\)。
- 一步**落子** \(v\) 合法，当且仅当 \(\mathrm{apply}(s,v,p)\notin H\)。
- **虚手不受此限制**：只换执子方、不改占领。
- \(H\) 含执子方，故「局面」= 占领 + 谁的回合。

采用**严格 SSK**：任何落子导致的精确局面重复都禁（不区分「是否还有别的合法着」）。  
**不实现对称性重复**：只对逐顶点相等的 \((\mathrm{occ},\,p_{\mathrm{move}})\) 判劫。环面上「平移后相同」在传统意义上也可算劫，但判定图自同构等价接近可能性死活，故不做。训练侧打乱合法着、按网络分派顶点重编号（GNN/1DCNN/MLP 随机 \(S_n\)、2DCNN 走棋盘对称；GNN 并置换邻接），均不进入规则引擎。

---

## 5. 终局与胜利（中国数子）

### 5.1 虚手、出局与结束

- 虚手始终合法。
- 连续两手虚手 ⇒ 出局（不算负）。
- **协议结束**：所有未出局方最近一手都是虚手。
- **倒数第二人出局**：只剩两人时一人出局，整盘结束。

动机：禁止无限落子、使对局永远到不了协议结束。虚手是结束手段，不是要禁的拖延。出局不等于判负，胜负由计分决定。

### 5.2 焦点扩张

结束后各方**依次**占领自己的全部焦点（无入边的空点不是焦点，跳过）。此阶段不提子。迭代到不动点。

### 5.3 势力范围与计分

焦点扩张之后：

- 对空顶点 \(v\)：若非空入邻居全属同一方 \(p\)，则 \(v\) **由 \(p\) 占领**（真正落子，不是只计分）。
- **同时进行**：每一轮基于同一快照判定并一次性占领，禁止边填边用新值。
- **迭代到不动点**，直到没有空点的入邻居只含单方势力。
- 剩余空点：入邻居含 \(\ge 2\) 方则**平分**；入邻居全空则该点不计分。

已被占领的点不会被对方用焦点或势力范围吞并。

每方分数 = 终局占领顶点数（含两阶段扩张）+ 平分份额 + 贴目。

**不先提死子再数。** 成熟棋手官子结束后不留死子（该提的已提、该弃的已弃），故终局不再走「提死子」。在线图等退化图上强行提死子可能整块消失。终局路径固定为：焦点扩张 → 势力范围 → 平分，中间不提子。

### 5.4 贴目

贴目是各方分数上的常数项（实现里按「从该方终局分中减」写入 `komi_schedule`，末位固定 \(0\)）。对局胜负按当时给定的序列判定；训练不使用贴目，见实现文档。

**引擎默认全 0。** C++ `Game` 的 `komi` 参数默认为空，再 `komi_schedule.resize(np, 0.f)`，各方贴目均为 0。训练、Arena、Elo 也传空序列——它们比较的是「优势」（§5.5 的子数差，相对量），本就不需要贴目；只有展示对局的「报胜负」才用贴目。下文 \([-7.5, 0]\) 以及三人、四人递减序列是 **Web UI / 展示对局** 的默认，不是 `Game()` 构造默认。

二人：数子法下先手贴 \(3\frac{3}{4}\) 子，等价于黑白相差 7.5 子。UI 默认**只扣先手**：`komi_schedule = [-7.5, 0]`。黑赢 \(\iff\) 黑子数 − 白子数 \(> 7.5\)。

多人尚无公认常数，UI 大致按行棋顺序递减、末手不贴：

- 三人局大概贴 \([7.5,\,4.5,\,0]\)，即 `komi_schedule = [-7.5, -4.5, 0]`。
- 四人局大概贴 \([7.5,\,4.5,\,2.5,\,0]\)，即 `komi_schedule = [-7.5, -4.5, -2.5, 0]`。

### 5.5 优势与先手优势（备注）

**优势（score lead）** 是某方领先多少的**相对量**，零点 = 双方子数相等（均势），不含贴目。设各方分数和为 \(S=\sum_p \mathrm{score}_p\)（终局每个顶点贡献恰 1，故 \(S=n\)），则方 \(p\) 的优势：

\[
\mathrm{lead}_p = \frac{k\cdot \mathrm{score}_p - S}{(k-1)\,n}\;\in[-1,1]
\]

二人 \(k=2\) 退化为 \((\mathrm{score}_p-\mathrm{score}_q)/n\)，即「\(p\) 比对手多占的点数 / 总点数」。

**优势 vs 胜负**：优势是相对量（谁领先、领先多少），胜负是绝对量（分数 + 贴目后比大小）。训练、MCTS 选点、Arena、Elo 只比较优势，因此**不依赖贴目**；只有终局「报谁赢」才需要贴目。

**先手优势 δ**：无贴目下，先手对后手的期望分数差 \(\delta=\mathbb E[\mathrm{score}_{\text{先手}}-\mathrm{score}_{\text{后手}}]\)（单位：点）。δ 是**图的函数**：网格图 δ>0 且较大；环面（无边界、平移对称）δ≈0；**随机图未知，必须逐图实测**。

**公平贴目 = δ**：给后手加 δ 点（`komi_schedule` 里后手项 −δ）即得均势 \(\mathbb E[\mathrm{score}_{\text{先手}}]=\mathbb E[\mathrm{score}_{\text{后手}}]\)。标准 19×19 网格的 7.5 子正是一类 δ 的实测值，**只对网格成立，不可套用到乱图**。

**交替先后手**：对弈双方各执一半先手时，δ 作为加性项在优势的期望里抵消（\(E[\mathrm{lead}]=\text{棋力差}/n\)），故 Arena / Elo 无需知道 δ 也能公平比较强弱。但这只消除了偏置：若用 0/1 胜负（而非连续优势），δ 巨大时（如线图）「谁先谁赢」会把棋力差截断，故必须用连续优势信号。

---

## 6. 多人

顶点占领、轮流、虚手/出局、势力范围平分均推广到 \(k\ge 3\)。焦点/弱点/眼不依赖人数。贴目：引擎默认全 0；UI 展示用 §5.4 的递减序列。实现支持 2/3/4 人对局；训练默认 \(k=2\)，也可用 `--players` 训 3/4 人网（与 2 人网分开）。

---

## 7. 与传统围棋对照

| 项 | 传统 19×19 | 本规则 |
|---|---|---|
| 棋盘 | 正方形网格 | 任意有向图 |
| 玩家 | 2 | \(k\ge 2\) |
| 邻接 | 4-邻接无向 | 任意有向邻接 |
| 气 | 空邻居 | 空入邻居 |
| 焦点=弱点=眼 | 是 | 仅无向图上 |
| 自杀 | 禁（提完后己方仍须有气） | 同；有向图上「提了仍无气」也禁 |
| 劫 | 局面超级劫 | 同（精确重复，无对称性） |
| 终局扩张 | 是 | 焦点 + 势力范围 |
| 贴目 | 单值 | 序列；引擎默认全 0，UI 二人只扣先手（三/四人见 §5.4） |
